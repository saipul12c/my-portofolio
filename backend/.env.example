# Copy to .env and fill the values
# This backend uses JSON files to store data (no external DB required).
# JWT secret for signing auth tokens (change for production):
JWT_SECRET=change-me-to-a-strong-secret
# Optional: allow origins for CORS (comma separated) e.g. http://localhost:5173
ALLOWED_ORIGINS=
PORT=8080
